# 部署到云（方案 B：永久在线，链接固定）

把打卡系统部署到云平台后，你将获得：

- ✅ **固定网址**（如 `yorke-checkin.onrender.com`），30 天不变，不用每天重发链接
- ✅ **24 小时在线**，不依赖你电脑开机
- ✅ **数据持久保存**（数据库 + 上传照片存在云盘，重启不丢）
- ✅ 微信里直接点开，和现在的隧道链接体验一样

> 前提：需要一个 GitHub 账号（免费）和一个 Render 账号（免费）。整个过程约 10 分钟，无需写代码。

---

## 方法一：Render + Docker（推荐，最简单）

### 1. 把代码推到 GitHub
1. 打开 https://github.com → 注册/登录 → 右上角 **New repository**
2. 仓库名填 `yorke-checkin`，选 **Public**，点 Create repository
3. 在本机项目目录（`yorke-checkin/`）打开命令行，依次执行：
   ```bash
   git init
   git add .
   git commit -m "init"
   git branch -M main
   git remote add origin https://github.com/你的用户名/yorke-checkin.git
   git push -u origin main
   ```
   （如果不会用 git，可用 GitHub 网页端直接把文件夹拖上去上传）

### 2. 在 Render 一键部署
1. 打开 https://render.com → 注册（可用 GitHub 直接登录）
2. 登录后点 **New** → **Blueprint**
3. 关联你的 GitHub，选中 `yorke-checkin` 仓库
4. Render 会自动读取 `render.yaml`：
   - 用 `Dockerfile` 构建
   - 挂 1GB 持久盘到 `/var/data/yorke`（存数据库和照片）
   - 自动分配固定域名
5. 点 **Deploy** 即可。首次构建约 2–5 分钟。

### 3. 拿到固定链接
部署完成后，Render 控制台会显示一个地址，类似：
```
https://yorke-checkin.onrender.com
```
把它发到群里，销售就能永久访问了。

> 💰 费用：Render 免费套餐 + 1GB 持久盘，30 天约 **$0.15（≈1 元人民币）**，几乎免费。免费套餐在无人访问约 15 分钟后会休眠，下次有人打开时自动唤醒（约几秒）。

---

## 方法二：Render 原生 Python（不用 Docker）

如果你不想用 Docker，删掉仓库里的 `render.yaml`，保留 `Procfile` + `runtime.txt` + `requirements.txt`，然后在 Render 选 **New → Web Service**（不是 Blueprint），Build Command 填 `pip install -r requirements.txt`，Start Command 填 `python app.py`。持久盘需在 Render 控制台的 Disk 设置里手动挂到 `/var/data/yorke`。

---

## 免费替代：PythonAnywhere

如果你连那一两块钱都不想花，可用 PythonAnywhere 免费版（文件系统持久、不休眠）：
1. 注册 https://www.pythonanywhere.com （免费版）
2. 控制台 **Bash** 里 `git clone` 你的仓库，创建 virtualenv，`pip install -r requirements.txt`
3. 在 **Web** 里新建 Flask 应用，WSGI 文件指向 `app.py` 的 `app` 对象，Working directory 设为项目目录
4. 免费版会分配 `你的用户名.pythonanywhere.com` 固定域名

---

## 上云后要注意

| 项目 | 说明 |
|---|---|
| 管理员邀请码 | 仍是 `yorke2026`（在 `app.py` 里可改） |
| 成员名单 | 云上是全新数据库，**没有演示数据**，需要让销售自行首次登录注册 |
| 数据安全 | 数据库和照片都在持久盘，不会因重启丢失 |
| 备份 | 在 Render 控制台 Disk 页面可下载快照；或用后续的导出工具导出 CSV |
| 本地照常用 | 本地双击 `启动.bat` 仍正常（路径自动回退到 `./data`） |

---

## 环境变量说明（给运维用）
| 变量 | 默认值 | 作用 |
|---|---|---|
| `PORT` | 5000 | 云平台的监听端口（Render 自动注入） |
| `DATA_DIR` | `./data` | 数据库目录；云上设为持久盘路径 |
| `UPLOAD_DIR` | `./static/uploads` | 照片目录；云上设为持久盘路径 |
| `SECRET_KEY` | 内置默认值 | 会话签名密钥；云上建议用随机值 |
| `FLASK_DEBUG` | 未设置 | 设为 `1` 才开调试模式（仅本地用） |
